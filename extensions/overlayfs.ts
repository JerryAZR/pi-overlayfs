/**
 * pi-overlayfs — copy-on-write fork isolation for pi's file-touching tools.
 *
 * Thin extension entry; the implementation lives in src/overlay/.
 */
export { default } from "../src/overlay/index.js";
